"""ORM boundary for business activity audit events.

Persistence details are intentionally introduced in the foundational phase.
This module remains separate from :mod:`auth_audit_log` so the two audit
boundaries cannot be coupled accidentally.
"""

